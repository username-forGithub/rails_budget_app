<%= form_with(model: payment, :url => {action: :create}, class: 'save-newcat') do |form| %>

  <% if payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
        <% payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="catnewtitle">
    <%= form.text_field :name, placeholder: "Title" %>
  </div>

  <div class="catnewtitle">
    <%= form.number_field :mount, placeholder: 'Mount' %>
  </div>

   <div>
    <%= form.hidden_field :user_id, :value => current_user.id %>
  </div>

   <div>
    <%= form.hidden_field :category_id, :value => @cat_id %>
   </div>
    <div><% @ocat = cat_id.to_i.inspect %></div>
  <div class="new_cat">
    <div class="w-rectangle">
      <%= form.submit 'Save', class: "new" %>
    </div>
  </div>
<% end %>
