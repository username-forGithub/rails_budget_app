<%= form_with(model: payment, :url => {action: :create}) do |form| %>

  <% if payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
        <% payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :mount, style: "display: block" %>
    <%= form.number_field :mount %>
  </div>

   <div>
    <%= form.hidden_field :user_id, :value => current_user.id %>
  </div>

   <div>
    <%= form.hidden_field :category_id, :value => @cat_id %>
   </div>


<div><% @ocat = cat_id.to_i.inspect %></div>

   <%# <div>
    <%= form.hidden_field :ooo_id, :value => @ocat %>
   <%# </div> %>
   

  <div>
    <%= form.submit %>
  </div>
<% end %>
